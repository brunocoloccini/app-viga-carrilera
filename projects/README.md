# Local Project Workspaces

This folder stores local crane runway project workspaces generated by:

```bash
PYTHONPATH=src python scripts/init_crane_runway_project.py ...
```

Generated project folders are user/local outputs and may differ across users or machines.

Do not treat sample template cases as design recommendations; always perform independent engineering verification.
