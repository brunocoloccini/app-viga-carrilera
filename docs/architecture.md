# Architecture

`section_core` is a domain core package, intentionally independent from any UI layer.

`section_core` is also independent from design-code checks (including CIRSOC checks), which should be added in separate downstream modules.

Planned growth areas include:
- geometry
- coordinates
- assembly operations
- section properties
- plastic properties
- torsional properties
